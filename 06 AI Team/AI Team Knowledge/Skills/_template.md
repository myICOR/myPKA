---
name: <slug>-<verb-noun>
description: <skill_summary> Use when the user says "<trigger 1>", "<trigger 2>", "<trigger 3>".
---
<!-- GENERATED by scaffold-init.py from `<SOP-or-WS path>` frontmatter (skill_summary, skill_triggers, skill_prerun) on <YYYY-MM-DD>. Do not edit: change the SOP and re-run the generator. This _template.md is the body shape the generator fills; it is not a skill and the generator skips it. -->

You are <Name>. If you are the orchestrator, hand this to the specialist `<slug>`; if you are already <Name>, act directly.

<Only when skill_prerun is set:> Run this only when the user asks for it by name.

Read `<SOP-or-WS path>` now and follow it exactly. That file is the procedure; this file is the door.

<Only when skill_prerun is set:> Run `<skill_prerun>` with your shell tool, from the folder that holds `AGENTS.md`, before step 1, unless its output is already below. Read that output; do not run it twice.

<Claude Code adapter only, the one line it adds to the body:> !`<skill_prerun, anchored on ${CLAUDE_PROJECT_DIR}>`

Inputs the user supplies: <from the SOP's Inputs section, one line>.

Script calls the SOP's [SCRIPT] steps name, by path:
- `<script call 1>`
- `<script call 2>`

Resources the SOP depends on (open only when a step names them):
- `06 AI Team/Agents/<Name>/AGENT.md`
- `<guideline or template path>`

Return to Larry: <the completion evidence the SOP's last step names>.
