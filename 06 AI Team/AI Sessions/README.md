# AI Sessions

The record of your conversations with the AI Team. The chat writes here;
you only read.

One folder per conversation, holding the main transcript plus a transcript
for every subagent that conversation launched. Each transcript keeps its
original session id in frontmatter, so a conversation that ended days ago
can be picked up again exactly where it stopped.

Nothing here needs processing. It is a record, not an inbox.
